{
  "name": "workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "legacy-messaging",
    "curtain-site"
  ],
  "scripts": {
    "dev": "npm --prefix curtain-site run dev",
    "build": "npm --prefix curtain-site run build",
    "start": "npm --prefix curtain-site run start",
    "lint": "npm --prefix curtain-site run lint",
    "legacy": "npm --prefix legacy-messaging start"
  }
}
